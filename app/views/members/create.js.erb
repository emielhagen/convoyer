const updateCard = (userData) => {
  const element = document.getElementById('emieltest');
  element.innerHTML = userData;
  element.insertAdjacentHTML('afterbegin', reviewHTML);
}

updateCard('<%= j render "convoys/convoy_card", convoy: @convoy, able_to_join: false %>');
