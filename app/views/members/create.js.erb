function addReview(reviewHTML) {
  const reviews = document.querySelector('.reviews-count');
  reviews.innerHTML = reviewHTML;
  reviews.insertAdjacentHTML('afterbegin', reviewHTML);
}

addReview('<%= j render "reviews/reviewed", review: @review %>');

